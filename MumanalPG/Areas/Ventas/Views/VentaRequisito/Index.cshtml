@model IEnumerable<MumanalPG.Models.Ventas.vRequisito>

@{
    ViewData["Title"] = "Prestaciones / ASR / Contratacion / Requisitos";
}


@*<div class="row">
    <div class="col-6">
        <h3 @ViewBag.DatosCabecera"</h3>
    </div>
    <div class="col-6 text-right">
            <a asp-action="Create" asp-route-id="@TempData["IdContratacion"]" class="btn btn-info">&nbsp; Nuevo Requisito</a>
    </div>
</div>*@



<table class="table">
    <thead>
        <tr>
            <th>
                Documento
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PathArchivo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ArchivoCargado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Aprobado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Descripcion)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.PathArchivo)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ArchivoCargado)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Aprobado)
                </td>
                <td>
                    <a class="btn btn-info" asp-action="CargaArchivo" asp-route-id="@item.IdVentaRequisito">Cargar</a>
                    <a class="btn btn-info" asp-action="Escanear" asp-route-id="@item.IdVentaRequisito">Scan</a>
                    <a class="btn btn-info" asp-action="VerPDF" asp-route-id="@item.IdVentaRequisito">Ver</a>
                    <a class="btn btn-success" asp-action="Aprobar" asp-route-id="@item.IdVentaRequisito">Aprobar</a>
                </td>
            </tr>
        }
    </tbody>
</table>
